---@class util.bufferline.functions
local M = {}

-- function M.move_to_start(bufnr)
--   require("bufferline").exec(
--     4, -- the forth visible buffer from the left
--     user_function -- an arbitrary user function which gets passed the buffer
--   )
-- end

return M
