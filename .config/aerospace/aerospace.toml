### AeroSpace Window Manager Configuration
## Startup & Behavior
start-at-login = true
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []
after-startup-command = []

# Normalizations: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

## Default layout settings
# Possible values: tiles|accordion
default-root-container-layout = 'tiles'
# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'
# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

## Mouse
# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = ['move-mouse window-lazy-center']

## OSX
# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = true


## Exec
[exec]
inherit-env-vars = true
[exec.env-vars]
PATH = '/opt/homebrew/bin:/opt/homebrew/sbin:${PATH}'

## Keyboard
# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'

## Spacing
# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 0
inner.vertical = 0
outer.left = 0
outer.bottom = 0
outer.top = 0
outer.right = 0
# 8px padding
# inner.horizontal = 8
# inner.vertical = 8
# outer.left = 8
# outer.bottom = 8
# outer.top = 8
# outer.right = 8

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# Commands: https://nikitabobko.github.io/AeroSpace/commands#focus
[mode.main.binding]
# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
#                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
#                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
#                   sectionSign (ISO keyboards only, european keyboards only)
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift
# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# OMARCHY-INSPIRED CONFIGURATION
# Using cmd (Super) key as main modifier to match Omarchy's Super key behavior

# Disable macOS defaults that conflict
# Using `automatically-unhide-macos-hidden-apps` instead.
# cmd-h = []     # Disable "hide application"
cmd-alt-h = [] # Disable "hide others"

# Terminal launcher (Omarchy equivalent)
# Interferes with kitty "new window"
# cmd-enter = '''exec-and-forget osascript -e '
# tell application "Kitty"
# do script
# activate
# end tell'
# '''

# LAYOUT CONTROLS (Omarchy-inspired)
# cmd-j toggles horizontal layout (matches Omarchy's Super+J for horizontal toggle)
# these conflict with Format in raycast notes
ctrl-alt-j = 'layout tiles horizontal vertical'
ctrl-alt-t = 'layout accordion horizontal vertical' # Using cmd-t for navigation/layout (Omarchy uses Super+T)
alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'

# FOCUS
# Window focus (Omarchy style - Super + Arrow keys)
cmd-left = "focus --boundaries-action wrap-around-the-workspace left"
cmd-down = "focus --boundaries-action wrap-around-the-workspace down"
cmd-up = "focus --boundaries-action wrap-around-the-workspace up"
cmd-right = "focus --boundaries-action wrap-around-the-workspace right"

# Window focus (vim-style), works outside of Kitty
alt-h = "focus --boundaries-action wrap-around-the-workspace left"
alt-j = "focus --boundaries-action wrap-around-the-workspace down"
alt-k = "focus --boundaries-action wrap-around-the-workspace up"
alt-l = "focus --boundaries-action wrap-around-the-workspace right"
# alt-leftSquareBracket = "join-with right"
alt-r = 'mode resize'
# alt-rightSquareBracket = "join-with down"

# Window focus (vim-style) - Using Alt-Shift to prioritize nvim/tmux
# alt-shift-h = 'focus left'
# alt-shift-j = 'focus down'
# alt-shift-k = 'focus up'
# alt-shift-l = 'focus right'

# WINDOW MOVEMENT 
# Omarchy style (Omarchy uses Super + Shift + Arrow for swapping/moving)
cmd-shift-left = 'move left'
cmd-shift-down = 'move down'
cmd-shift-up = 'move up'
cmd-shift-right = 'move right'

# Vim style, works outside of Kitty
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Note:
# cmd-j is used for layout toggle (Omarchy's Super+J), not focus down
# cmd-k is used for raycast
# Alternative vim-style navigation (keeping for flexibility)

# RESIZING
cmd-shift-minus = 'resize smart -50'
cmd-shift-equal = 'resize smart +50'
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'
# For Kitty, where cmd-shift and alt-shift are already in use
ctrl-alt-equal = 'resize smart +50'
ctrl-alt-minus = 'resize smart -50'

# QUICK ACTIONS
# alt-f10 = 'fullscreen' # Omarchy uses f11, but this supports smaller keyboards
f10 = 'fullscreen'                                 # Omarchy uses f11, but this supports smaller keyboards
f9 = 'exec-and-forget ./scripts/aerospace-wm.fish'
# alt-o = 'focus-back-and-forth'
alt-o = 'focus-back-and-forth'
alt-tab = 'focus-back-and-forth'
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'
alt-shift-f = 'fullscreen'

# WORKSPACE SWITCHING (Omarchy uses Super + 1-0 for workspaces 1-10)
# Primary workspaces 1-9 and 0 (matching Omarchy's 1-10 pattern)
ctrl-cmd-alt-shift-1 = 'workspace 1'
ctrl-cmd-alt-shift-2 = 'workspace 2'
ctrl-cmd-alt-shift-3 = 'workspace 3'
ctrl-cmd-alt-shift-4 = 'workspace 4'
ctrl-cmd-alt-shift-5 = 'workspace 5'
ctrl-cmd-alt-shift-6 = 'workspace 6'
ctrl-cmd-alt-shift-7 = 'workspace 7'
ctrl-cmd-alt-shift-8 = 'workspace 8'
ctrl-cmd-alt-shift-9 = 'workspace 10'
ctrl-cmd-alt-shift-0 = 'workspace 10'                      # Using 10 for the 0 key to match Omarchy's 1-10 pattern
ctrl-cmd-alt-shift-leftSquareBracket = ['workspace prev']
ctrl-cmd-alt-shift-rightSquareBracket = ['workspace next']


# Extended workspaces (keeping some letter workspaces for flexibility)
# cmd-a = 'workspace A'
# cmd-b = 'workspace B'
# cmd-c = 'workspace C'
# cmd-d = 'workspace D'
# cmd-e = 'workspace E'
# cmd-f = 'workspace F'
# cmd-g = 'workspace G'
# cmd-i = 'workspace I'
# cmd-m = 'workspace M'
# cmd-n = 'workspace N'
# cmd-o = 'workspace O'
# cmd-p = 'workspace P'
# cmd-q = 'workspace Q'
# cmd-r = 'workspace R'
# cmd-s = 'workspace S'
# cmd-u = 'workspace U'
# cmd-v = 'workspace V'
# cmd-w = 'workspace W'
# cmd-x = 'workspace X'
# cmd-y = 'workspace Y'
# cmd-z = 'workspace Z'

# MOVE WINDOWS TO WORKSPACES (Omarchy uses Shift + Super + 1-4)
# Primary workspace movement (matching Omarchy pattern)
cmd-ctrl-shift-1 = ['move-node-to-workspace 1', 'workspace 1']
cmd-ctrl-shift-2 = ['move-node-to-workspace 2', 'workspace 2']
cmd-ctrl-shift-3 = ['move-node-to-workspace 3', 'workspace 3']
cmd-ctrl-shift-4 = 'move-node-to-workspace 4'
cmd-ctrl-shift-5 = 'move-node-to-workspace 5'
cmd-ctrl-shift-6 = 'move-node-to-workspace 6'
cmd-ctrl-shift-7 = 'move-node-to-workspace 7'
cmd-ctrl-shift-8 = 'move-node-to-workspace 8'
cmd-ctrl-shift-9 = 'move-node-to-workspace 9'
cmd-ctrl-shift-0 = 'move-node-to-workspace 10'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
cmd-ctrl-shift-rightSquareBracket = [
  'move-node-to-workspace prev',
  'workspace prev',
]
cmd-ctrl-shift-leftSquareBracket = [
  'move-node-to-workspace next',
  'workspace next',
]

# WORKSPACE AND MONITOR MANAGEMENT
cmd-tab = 'workspace-back-and-forth'
cmd-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# SERVICE MODE (keeping semicolon for service mode)
cmd-shift-semicolon = 'mode service'
cmd-shift-pageUp = 'mode service'

# SERVICE BINDING
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
enter = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = [
  'layout floating tiling',
  'mode main',
] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']
# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']
down = 'volume down'
up = 'volume up'
shift-down = ['volume set 0', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# RESIZE BINDING
[mode.resize.binding]
alt-h = 'resize width -50'
alt-j = 'resize height +50'
alt-k = 'resize height -50'
alt-l = 'resize width +50'
alt-r = 'mode main'
enter = 'mode main'
esc = 'mode main'

# WINDOW DETECTIONS

# Scrypted
[[on-window-detected]]
if.app-id = 'com.electron.scrypted'
check-further-callbacks = true
run = ['move-node-to-workspace 3']

# Obsidian
[[on-window-detected]]
if.app-id = 'md.obsidian'
check-further-callbacks = true
run = ['move-node-to-workspace 2']

# Raycast
[[on-window-detected]]
if.app-id = 'com.raycast.macos'
if.window-title-regex-substring = 'AI Chat'
run = 'layout tiling'


[[on-window-detected]]
if.app-id = 'com.jetbrains.rustrover'
check-further-callbacks = true
run = ['move-node-to-workspace 4']


[[on-window-detected]]
if.app-id = 'net.kovidgoyal.kitty'
check-further-callbacks = true
run = ['move-node-to-workspace 1']

[[on-window-detected]]
if.app-id = 'app.zen-browser.zen'
check-further-callbacks = true
run = ['move-node-to-workspace 1']

### FLOATING WINDOWS
# System preferences
[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
check-further-callbacks = true
run = ['layout floating']

# Preview
[[on-window-detected]]
if.app-id = 'com.apple.Preview'
check-further-callbacks = true
run = ['layout floating']

# Dash
[[on-window-detected]]
if.app-id = 'com.kapeli.dashdoc'
check-further-callbacks = true
run = ['layout floating', 'move-node-to-workspace 1']

# 1Password
[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.raycast.macos'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'hammerspoon'
check-further-callbacks = true
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'org.pqrs.Karabiner-Elements.Settings'
check-further-callbacks = true
run = ['layout floating']
### /FLOATING WINDOWS
